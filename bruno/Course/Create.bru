meta {
  name: Create
  type: http
  seq: 3
}

post {
  url: http://127.0.0.1:8001/api/v1/tables/{{firstTableId}}/courses
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

script:post-response {
  bru.setVar("firstCourseId", res.body?.item?.id);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
