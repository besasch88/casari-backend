meta {
  name: Get
  type: http
  seq: 4
}

get {
  url: http://127.0.0.1:8001/api/v1/tables/{{firstUseCaseId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

script:post-response {
  bru.setVar("firstTableId", res.body?.item?.id);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
