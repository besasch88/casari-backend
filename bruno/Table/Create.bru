meta {
  name: Create
  type: http
  seq: 5
}

post {
  url: http://127.0.0.1:8001/api/v1/tables
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "7-Finestra"
  }
}

script:post-response {
  bru.setVar("firstTableId", res.body?.item?.id);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
