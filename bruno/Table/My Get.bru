meta {
  name: My Get
  type: http
  seq: 5
}

get {
  url: http://127.0.0.1:8001/api/v1/my/tables/{{firstUseCaseId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

script:post-response {
  bru.setVar("firstUseCaseId", res.body?.item?.id);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
