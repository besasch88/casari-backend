meta {
  name: Create
  type: http
  seq: 3
}

post {
  url: http://127.0.0.1:8001/api/v1/menu/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "title": "Title"
  }
}

script:post-response {
  bru.setVar("firstMenuCategoryId", res.body?.item?.id);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
