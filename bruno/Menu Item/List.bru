meta {
  name: List
  type: http
  seq: 1
}

get {
  url: http://127.0.0.1:8001/api/v1/menu/categories/{{firstMenuCategoryId}}/items
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

script:post-response {
  bru.setVar("firstMenuItemId", res.body?.items[0]?.id);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
